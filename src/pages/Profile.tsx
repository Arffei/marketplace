import { Typography } from '@mui/material';

// Минимальная реализация
const Profile = () => {
  return (
    <div>
      <Typography variant="h4">Личный кабинет</Typography>
    </div>
  );
};

export default Profile; // Добавляем экспорт