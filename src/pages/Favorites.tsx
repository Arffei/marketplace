import { Typography } from '@mui/material';

const Favorites = () => {
  return (
    <div>
      <Typography variant="h4">Избранное</Typography>
      {/* Добавьте содержимое страницы */}
    </div>
  );
};

export default Favorites; // Добавляем экспорт