// theme.ts
import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#2e7d32',
    },
    secondary: {
      main: '#ffab00',
    },
  },
  typography: {
    fontFamily: 'Roboto, Arial',
  },
});