import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffa000', // Основной цвет
    },
    secondary: {
      main: '#ffffff', // Второстепенный цвет
    },
  },
});

export default theme;